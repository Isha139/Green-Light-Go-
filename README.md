# Green-Light-Go-
A small JavaScript project that simulates a real traffic signal. Each time the button is pressed (or the function runs), the lights cycle between red → yellow → green using basic DOM manipulation.
